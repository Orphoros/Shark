let a: string = "Hello World";
let b: bool = true;
let c: i64 = 55555;
let d: func<(i64)->i64> = (x: i64): i64 => { x + 10; };
let e: array<any> = [1, false, "John"]
let f: hashmap<string,any> = { "name": "John", "age": 25 }
let length = len(a);
puts(length);

let sumOfAP = fn(a, d, n) {
    let sum = 0;
    let i = 0;

    while (i < n) {
        sum += a;
        a += d;
        i++;
    }

    return sum;
}

let a = 1;
let d = 7;
let n = 1500;

puts(sumOfAP(a, d, n));
let sumOfAP = (mut a=0, d=0, n=0) => {
    var sum = 0;
    let mut i = 0;

    while (i < n) {
        sum += a;
        a += d;
        i++;
    }

    return sum;
}

let a = 1;
let d = 7;
let n = 1500;

puts(sumOfAP(a, d, n));
